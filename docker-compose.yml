version: '3'
services:
  client:
    image: registry.gitlab.com/kanehara/mixceed/client:latest
    ports:
      - "8080:8080"
  server:
    image: registry.gitlab.com/kanehara/mixceed/server:latest
    ports:
      - "4000:4000"
    